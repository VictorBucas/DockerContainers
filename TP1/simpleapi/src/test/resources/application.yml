spring:
  datasource:
    url: jdbc:postgresql://postgre_db:5432/db
    username: usr
    password: pwd
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.format_sql: true
server:
  port: 8080
